# Workspace Hermes 운영 규칙

이 파일은 Hermes 전용 기본 규칙이다. 이 프로젝트에서 Hermes가 Desktop, Discord, cron으로 Workspace/PlayAuto 자동화를 할 때 항상 따른다.

## 응대 방식

- 사용자는 비개발자다. 한국어로 짧고 쉽게, 결과와 영향 중심으로 설명한다.
- 코드/명령어 세부사항은 필요한 경우에만 간단히 말한다.
- 사용자가 "알아서 해줘"라고 하면 안전한 범위에서 먼저 실행한다.
- 결제, 삭제, 권한 변경, 운영 DB 변경, 예상 범위를 벗어난 외부 전송은 실행 직전 확인한다.
- 이미 안정화된 반복 업무는 사용자가 시작 지시를 하면 중간 확인 없이 끝까지 진행하되, 건수/대상 불일치가 있으면 멈추고 보고한다.

## 프로젝트 기본 정보

- 프로젝트 경로: `C:/Users/kht33/Desktop/workspace`
- 로컬 앱: `http://localhost:3000/workspace/`
- 개발 서버: `npm run dev`
- 검증 명령: `npm run typecheck`, `npm run lint`, `npm run verify`
- 코드 작업 전후에는 필요 시 `AGENTS.md`도 확인한다.
- 비밀정보(`.env.local`, API 키, 토큰, 비밀번호, 쿠키, 세션값)는 출력/저장/커밋하지 않는다.

## 자동화 운영 원칙

- Workspace/PlayAuto 업무는 설명만 하지 말고 실제 브라우저/파일/작업관리 결과로 검증한다.
- 결과 보고에는 단순 성공/실패 숫자만 쓰지 말고 가능한 주문별 목록을 포함한다.
- 반복 절차와 실수 방지 규칙은 skill에 저장/업데이트한다.
- 장기 운영 규칙, 채널 운영 규칙, cron 운영 규칙은 이 `.hermes.md`에 짧게 반영한다.
- 세부 클릭 절차는 이 파일에 길게 넣지 말고 해당 skill 또는 `references/`에 둔다.

## Discord 채널 운영

- Discord는 휴대폰 명령/결과 보고용이다. 실제 자동화는 이 Windows PC의 Hermes Gateway와 Chrome 프로필에서 실행된다.
- 채널별로 업무를 분리한다.
  - `#운송장수집-자동화`: `workspace-reseller-automation` 우선 사용.
  - `#플레이오토발주서등록-자동화`: `playauto-order-import` 우선 사용.
  - `#구매자동화`: 구매자동화 절차 학습 후 별도 skill로 저장.
  - `#가격재고-자동화`: 가격/재고 절차 학습 후 별도 skill로 저장.
- Discord에서 `/new` 후에도 사용자가 채널명/업무명으로 요청하면 위 채널 규칙과 관련 skill을 우선 떠올린다.
- Discord 명령이 애매하면 반복 실행하지 말고 현재 단계와 다음 단계만 간단히 확인한다.

## cron 운영

- cron은 기존 대화 스레드를 그대로 이어받는 것이 아니라 새 실행으로 돈다.
- cron 작업에는 반드시 자체 완결형 프롬프트와 관련 skill을 연결한다.
- cron 결과는 업무별 Discord 채널로 전달한다.
- cron 결과에 사용자가 답장할 수 있게 필요한 경우 `attach_to_session=true`를 사용한다.
- cron 프롬프트에는 성공/중단 기준, 보고 형식, 0건 처리 규칙을 명확히 적는다.

## 운송장수집 자동화 핵심 규칙

- 기본 skill: `workspace-reseller-automation`
- 흐름: Workspace 배송조회수집 → 엑셀 다운로드/파싱 → PlayAuto 배송정보 업로드 → 홈 카드 `운송장출력 N` 확인 → 출고완료 → 운송장 전송 → 작업관리 확인.
- 배송조회수집 성공 0건이면 즉시 중단하고 `등록할 운송장이 없습니다`라고 보고한다.
- 성공 1건 이상이면 다시 수집하지 말고 다음 단계인 엑셀/PlayAuto로 진행한다.
- PlayAuto 상태 판단은 직접 링크의 초기 카운트보다 홈 카드 숫자를 우선한다.
- 운송장수집 자동화 완료 보고는 현재 채팅에 답한 뒤 Discord `#운송장수집-자동화`에도 `hermes send --to discord:운송장수집-자동화`로 전송한다.

## 플레이오토 발주서 등록 핵심 규칙

- 기본 skill: `playauto-order-import`
- 흐름: PlayAuto 주문수집 → 작업관리 완료 확인 → 발주양식 Excel 다운로드 → Workspace 결제 전 발주서 중복 제외 가져오기 → 신규 등록 주문 보고.
- `전체 가져오기`가 아니라 `중복 제외 N건 가져오기`를 우선한다.

## 학습형 운영 규칙

- 사용자가 절차를 정정하거나 자동화가 실패/개선되면 즉시 관련 skill을 패치한다.
- 여러 번 반복될 운영 규칙이면 `.hermes.md`에도 짧게 반영한다.
- 일회성 작업 진행상황, 주문번호/작업번호처럼 금방 낡는 정보는 memory나 `.hermes.md`에 저장하지 않는다.
- 비밀정보는 어떤 학습 저장소에도 저장하지 않는다.
